{
  "scripts": {
    "cypress:open": "npm run set_as_unchangeable && npx cypress open",
    "set_as_unchangeable": "git update-index --assume-unchanged cypress/fixtures/credentials.json",
    "set_as_changeable": "git update-index --no-assume-unchanged cypress/fixtures/credentials.json"
  },
  "dependencies": {
    "glob": "^8.1.0",
    "glob-promise": "^6.0.7",
    "pdf-parse": "^1.1.1",
    "puppeteer": "^24.12.1",
    "terser": "^5.39.0"
  },
  "devDependencies": {
    "cypress": "^14.5.0",
    "cypress-file-upload": "^5.0.8"
  }
}
